import * as React from "react";
import Layout from "../components/layout";

const AboutPage = () =>{
    return(
        <Layout pageTitle="About Page">
            <p>This text is comming from About Page.</p>
        </Layout>
    );
}

export default AboutPage;